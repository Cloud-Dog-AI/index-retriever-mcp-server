# Core runtime
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0.1,<7.0
ruamel.yaml>=0.18.6,<1.0
typing-extensions>=4.10,<5.0

# API-first service (HTTP API + admin web + MCP/A2A)
fastapi>=0.112,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-multipart>=0.0.30,<1.0      # uploads
sse-starlette>=2.1,<3.0           # SSE streaming (optional)
websockets>=12.0,<13.0            # WS streaming (optional)

# AuthN/AuthZ / RBAC
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7.4,<2.0
authlib>=1.3,<2.0                 # OIDC integration (Keycloak, etc.)

# Observability
rich>=13.7,<14.0

# Storage for profiles/queue/audit (choose one DB; Postgres recommended)
SQLAlchemy>=2.0.30,<3.0
alembic>=1.13,<2.0
asyncpg>=0.29,<1.0                # Postgres async driver
aiosqlite>=0.20,<1.0              # local dev option
filelock>=3.15,<4.0

# Job / queue management (simple DB-backed) + optional Redis multiplier
tenacity>=8.2,<9.0                # retries/backoff

# LlamaIndex (core wrapper). Keep adapters modular.
# Optional local HF embeddings stay out of the base image because current 0.2.x
# releases pin against llama-index-core<0.11 and break the 0.11 runtime line.
# Optional Ollama adapter also targets the older 0.10 core line in 0.2.x.
# Optional: LangChain support if required
langchain>=0.2,<0.4
langchain-community>=0.2,<0.4

# Vector DB clients (enable via extras / profile config)
chromadb>=1.0,<2.0
qdrant-client>=1.9,<2.0
weaviate-client>=4.6,<5.0
psycopg[binary]>=3.2,<4.0          # Postgres sync tooling (optional)

# Content ingestion / conversion
pypandoc>=1.13,<2.0
pdfminer.six>=20231228
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
odfpy>=1.4,<2.0
beautifulsoup4>=4.12,<5.0
lxml==5.2.2
xmlsec==1.3.17
markdown-it-py>=3.0,<4.0
chardet>=5.2,<6.0

# Connectors / remote references
boto3>=1.34,<2.0                  # S3
google-api-python-client>=2.140,<3.0
google-auth>=2.33,<3.0
requests>=2.32,<3.0               # general remote fetch
aiohttp>=3.9,<4.0                 # async fetch (optional)

# Hashing / content fingerprinting
xxhash>=3.4,<4.0                  # fast hashes (optional)

# Testing / quality (dev)
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
